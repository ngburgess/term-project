# Adaptive Filtering for Image Noise

## Instructions

**Compile**: gcc adaptive_noise_filter.c -o adaptive_noise_filter

**Run**: ./adaptive_noise_filter <input_image> <output_image>

**Shell Script** (compile + run on all test images): ./run_test.sh